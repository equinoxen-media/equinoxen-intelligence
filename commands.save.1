cd ~/equinoxen-intelligence
git status

git add .
git commit -m "Update intelligence and content pipeline scripts"
#push to github
git push origin main 

#pull from github
git pull origin main

#Step Back 
git reset --soft HEAD~1
git restore --staged <file-path>
git commit

#cron updates
crontab -e
crontab -l

#get LinkedIn code
https://www.linkedin.com/oauth/v2/authorization?response_type=code&client_id=86k0i096lhhnp3&redirect_uri=https://equinoxen.com&scope=openid%20profile%20w_member_social%20w_organization_social

#Lists every cached product with its score, assessed date, and days remaining before auto-reassessment
python3 content_pipeline.py ratings
#Manually forces a reassessment
python3 content_pipeline.py research           # see everything cached, with age remaining
python3 content_pipeline.py clear-research FreshBooks   # force a re-search before the year is up



python3 audit_claims.py --list                    # see what's auditable
python3 audit_claims.py kartra-review              # audit one post by slug
python3 audit_claims.py --all 20                   # audit your 20 most recent posts

python3 rewrite_article.py kartra-review

